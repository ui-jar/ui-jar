{
  "$schema": "http://json-schema.org/schema",
  "id": "AddUIJarDoc",
  "title": "Add Ui Jar Doc Options Schema",
  "type": "object",
  "description": "Adds an ui-jar configuration for a project or a library.",
  "properties": {
    "project": {
      "description": "The name of the project or the library.",
      "type": "string",
      "format": "html-selector",
      "x-prompt": "What is the name of the project or the library ?"
    }
  },
  "required": ["project"]
}
